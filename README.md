# Wiktionary-Json-Parse

Wiktionary Json Parse is a Python program that parses a large JSON file gotten from kaikki.org containing English dictionary entries into an SQLite database, removing unneeded attributes
from the file.
